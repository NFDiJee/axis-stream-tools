# axis-stream-tools
Free ACAP for Axis network cameras: YouTube Live RTMP streaming with bundled FFmpeg, auto-reconnect, audio file management, email   notifications, live monitoring (bitrate, CPU, temperature), settings backup — runs directly on the camera, no server required
